{
	"name": "linemedrd",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"dev": "concurrently -n \"server,client\" -c 'blue,green' \"cd server && npm start:dev\" \"cd client && npm start\"",
		"i-client": "cd client && npm install",
		"i-server": "cd server && npm install",
		"heroku-prebuild": "npm run i-server",
		"heroku-postbuild": "npm run i-client",
		"start": "cd server && npm start"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"husky": {
		"hooks": {
			"pre-commit": "pretty-quick --staged"
		}
	},
	"devDependencies": {
		"concurrently": "^5.3.0",
		"husky": "^4.3.0",
		"prettier": "^2.1.2",
		"pretty-quick": "^3.1.0"
	}
}
