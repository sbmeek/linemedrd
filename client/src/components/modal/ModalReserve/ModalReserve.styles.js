import styled from '@emotion/styled';
import ContainerMain from '@/shared/container/Container';

export const ButtonIcon = styled.div`
	width: auto;
`;

export default styled(ContainerMain)`
	padding-top: 3.5rem;
	z-index: 90;
	position: fixed;
`;
